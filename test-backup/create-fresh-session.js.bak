// Create fresh session data
const freshSessionData = {
  "user_id": "steam-76561198001993310",
  "email": "76561198001993310@steam.local",
  "role": "user",
  "expires_at": Date.now() + (60 * 60 * 24 * 7 * 1000) // 7 days from now
};

console.log('Fresh session data:', freshSessionData);
console.log('Expires at:', new Date(freshSessionData.expires_at).toISOString());

// URL encode it
const encoded = encodeURIComponent(JSON.stringify(freshSessionData));
console.log('URL encoded:', encoded);